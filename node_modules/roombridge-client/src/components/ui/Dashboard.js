import React, { useContext, useState } from "react";
import { Card, Button, Badge } from "@nextui-org/react";
import { UserContext } from "../../context/userContext";
import AddRoomAvailModal from "../AddRoomAvailModal";

function Dashboard() {
  const { user, isLogin } = useContext(UserContext);
  const [showAddRoomModal, setShowAddRoomModal] = useState(false);

  if (!isLogin || !user) {
    return (
      <div className="flex justify-center items-center min-h-screen">
        <Card className="p-6">
          <h3 className="text-xl font-semibold">Please log in to view your dashboard</h3>
        </Card>
      </div>
    );
  }

  return (
    <div className="max-w-7xl mx-auto p-6">
      <div className="mb-8">
        <h1 className="text-3xl font-bold text-gray-900">
          Hello, {user.full_name}! 👋
        </h1>
        <p className="text-gray-600 mt-2">
          Welcome to your RoomBridge dashboard. Here's what's happening with your account.
        </p>
      </div>

      <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
        {/* User Profile Card */}
        <Card className="p-6">
          <div className="flex items-center space-x-4">
            <img
              src={user.profile_picture_url || "https://tailwindcss.com/img/erin-lindford.jpg"}
              alt="Profile"
              className="w-16 h-16 rounded-full"
            />
            <div>
              <h4 className="mb-1 text-lg font-semibold">{user.full_name}</h4>
              <p className="text-gray-500">@{user.username}</p>
              <p className="text-sm text-gray-400">{user.email}</p>
            </div>
          </div>
        </Card>

        {/* Account Info Card */}
        <Card className="p-6">
          <h4 className="mb-4 text-lg font-semibold">Account Information</h4>
          <div className="space-y-2">
            <div className="flex justify-between">
              <span className="text-gray-600">Username:</span>
              <span className="font-medium">{user.username}</span>
            </div>
            <div className="flex justify-between">
              <span className="text-gray-600">Mobile:</span>
              <span className="font-medium">{user.mobile_number}</span>
            </div>
            <div className="flex justify-between">
              <span className="text-gray-600">Gender:</span>
              <span className="font-medium">{user.gender || "Not specified"}</span>
            </div>
            <div className="flex justify-between">
              <span className="text-gray-600">Rewards:</span>
              <Badge color="success">{user.rewards || 0} points</Badge>
            </div>
          </div>
        </Card>

        {/* Quick Actions Card */}
        <Card className="p-6">
          <h4 className="mb-4 text-lg font-semibold">Quick Actions</h4>
          <div className="space-y-3">
            <Button 
              className="w-full" 
              color="primary"
              onClick={() => setShowAddRoomModal(true)}
            >
              ➕ Add Room
            </Button>
            <Button 
              className="w-full" 
              color="default" 
              variant="flat"
              onClick={() => window.location.href = '/listed-rooms'}
            >
              Browse Rooms
            </Button>
            <Button 
              className="w-full" 
              color="secondary" 
              variant="flat"
              onClick={() => window.location.href = '/profile'}
            >
              Edit Profile
            </Button>
            <Button 
              className="w-full" 
              color="success" 
              variant="flat"
              onClick={() => window.location.href = '/events'}
            >
              View Events
            </Button>
          </div>
        </Card>
      </div>

      {/* Additional Information */}
      <div className="mt-8">
        <Card className="p-6">
          <h4 className="mb-4 text-lg font-semibold">Account Status</h4>
          <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
            <div className="text-center">
              <h3 className="text-blue-600 text-xl font-bold">Active</h3>
              <p className="text-gray-600">Account Status</p>
            </div>
            <div className="text-center">
              <h3 className="text-green-600 text-xl font-bold">{user.rewards || 0}</h3>
              <p className="text-gray-600">Reward Points</p>
            </div>
            <div className="text-center">
              <h3 className="text-purple-600 text-xl font-bold">
                {new Date(user.created_at).toLocaleDateString() || "N/A"}
              </h3>
              <p className="text-gray-600">Member Since</p>
            </div>
          </div>
        </Card>
      </div>
      
      {/* Add Room Modal */}
      {showAddRoomModal && (
        <AddRoomAvailModal setShowAddRoomModal={setShowAddRoomModal} />
      )}
    </div>
  );
}

export default Dashboard;
